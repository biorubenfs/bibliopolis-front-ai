# Puerto del frontend (solo para desarrollo local)
PORT=4200

# IMPORTANTE: Las URLs del backend se configuran en los archivos de environment:
# - src/environments/environment.ts (local: http://localhost:3000)
# - src/environments/environment.dev.ts (dev: http://dev.bibliopolis.es/api)
# - src/environments/environment.production.ts (prod: https://bibliopolis.es/api)
#
# Para buildear para un entorno específico:
# - Local dev: pnpm start
# - Dev: pnpm run build -- --configuration dev
# - Prod: pnpm run build -- --configuration production
#
# Docker build (usar BUILD_CONFIGURATION):
# docker build --build-arg BUILD_CONFIGURATION=dev -t bibliopolis-front:dev .
# docker build --build-arg BUILD_CONFIGURATION=production -t bibliopolis-front:prod .
#
# El contenedor expone el puerto 3000 (no 80)
# Nginx en el VPS hace proxy a localhost:3000
